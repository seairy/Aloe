<div class="chaos-panel grid_8">
  <div class="chaos-panel-header">
    <span>编辑大事记信息</span>
  </div>
  <div class="chaos-panel-body no-padding">
    <%= form_for [:admin, @memorabilia], html: { class: 'chaos-form' } do |f| %>
      <%= render "shared/admin/error_messages", target: @memorabilia %>
      <div class="chaos-form-inline">
        <div class="chaos-form-row bordered">
          <label class="chaos-form-label">年份</label>
          <div class="chaos-form-item">
            <%= f.text_field :year %>
          </div>
        </div>
        <div class="chaos-form-row bordered">
          <label class="chaos-form-label">内容</label>
          <div class="chaos-form-item">
            <%= f.kindeditor :content %>
          </div>
        </div>
      </div>
      <div class="chaos-button-row">
        <%= f.submit '提交', class: 'btn btn-danger' %>
        <input type="reset" value="重置" class="btn" />
        <%= chaos_button_to_return '返回大事记信息', path: url_for([:admin, @memorabilia]) %>
        <%= chaos_button_to_return '返回大事记列表', path: admin_memorabilias_path %>
      </div>
    <% end %>
  </div>
</div>