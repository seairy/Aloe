<div id="main">
  <div id="location"><%= location_tag %></div>
  <div class="link-list">
    <ul>
      <% @links.each do |link| %>
      <li>
        <%= link_to link.name, link.url, target: '_blank' %>
      </li>
      <% end %>
    </ul>
  </div>
</div>
<%= render 'side' %>